import React from 'react';

const Endpoints = () => (
  <>
    <h2>Endpoints</h2>
    <p>
      This page currently does nothing. In a future version, you will be able to create custom endpoints, with custom logic, 
      rather than using the default CRUD operations for all object types.
    </p>
  </>
)

export default Endpoints;
