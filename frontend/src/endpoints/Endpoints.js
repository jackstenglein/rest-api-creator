import React from 'react';

const Endpoints = () => (
  <p>
    This page currently does nothing. In a future version, you will be able to create custom endpoints, rather than using the default CRUD operations for all object types.
  </p>
)

export default Endpoints;
